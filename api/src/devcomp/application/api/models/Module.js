export class Module {
  constructor({ id, shortId, slug, title, duration, image }) {
    this.id = id;
    this.shortId = shortId;
    this.slug = slug;
    this.title = title;
    this.duration = duration;
    this.image = image;
  }
}
