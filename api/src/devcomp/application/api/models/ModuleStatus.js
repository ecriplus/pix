export class ModuleStatus {
  constructor({ id, slug, title, status, duration }) {
    this.id = id;
    this.slug = slug;
    this.title = title;
    this.status = status;
    this.duration = duration;
  }
}
