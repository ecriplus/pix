export class ModuleStatus {
  constructor({ id, slug, title, status, duration, image }) {
    this.id = id;
    this.slug = slug;
    this.title = title;
    this.status = status;
    this.duration = duration;
    this.image = image;
  }
}
