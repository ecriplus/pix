export class Module {
  constructor({ id, title, slug, duration, image }) {
    this.id = id;
    this.title = title;
    this.slug = slug;
    this.duration = duration;
    this.image = image;
  }
}
